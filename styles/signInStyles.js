
import {StyleSheet} from 'react-native';

const signInStyles = StyleSheet.create({
    instructionButton:{
        fontWeight: 700,
        color: '#FFFFFF',
        top:80,
        
    }
});

export default signInStyles;